function recursiveEven(number) {
    if (number === 0) {
        return 'even'
    }
    
    if (number === 1) {
        return 'odd'
    }

    return recursiveEven(number - 2)
}

// Collocation
function CombineCalculator({ prop }) {
    const [state, setState] = useSTate();
    const [anotehrState, anotherState] = useState();

    const calcualtedValue = combineCalculate(state, prop); // yseMneni 
  
   useWindowAPi();
   useWebSockeConnection();

   return <div></div>
}


const OPTIOSN= [];

function combineCacluclate(state: T StateModel, pro: numberp): number {
    return state + prop;
}


function MeetingRoutes() {
    return Outlet Route Route Route Route Route
}


// app.tsx

import MeetingRoutes 



<Rourte meetings component<MeetingRoutes />


index.ts
imports * from "../"